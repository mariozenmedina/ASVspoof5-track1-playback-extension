# Private research context; never distribute.
methodology.tex
.agent-context/

# Local acquisition outputs and operator-specific hardware bindings.
capture-tests/
capture-ledgers/
capture-failures/
.capture-work/
acquisition-config.*.json
.venv-capture/
__pycache__/
*.pyc

# Local ASVspoof 5 source audio. Kept on disk when available, never pushed to GitHub.
flac_T/
flac_D/
flac_E_eval/
